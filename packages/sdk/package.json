{
  "name": "@at1c/sdk",
  "version": "1.0.3",
  "description": "AT1C Protocol SDK — cryptographic consent and compliance receipts for AI agents.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc --project tsconfig.sdk.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepublishOnly": "npm run build && npm test"
  },
  "author": "AT1C Protocol",
  "license": "MIT",
  "dependencies": {
    "@noble/hashes": "^1.4.0",
    "@noble/post-quantum": "^0.7.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^4.1.11"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
